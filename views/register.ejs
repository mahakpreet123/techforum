<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/register.css">
  <title>Register</title>
</head>
<body>
  <div class="container">
    <div class="form-container">
      <h2>Sign up</h2>
      <form id="registerForm" action="/auth/register" method="POST">
        <div class="form-group mb-3">
          <span class="icon">👤</span>
          <input type="text" class="form-control" id="name" name="name" placeholder="Your Name" required pattern="^[A-Za-z\s]+$" title="Username should only contain letters and spaces.">
        </div>
        <div class="form-group mb-3">
          <span class="icon">📧</span>
          <input type="email" class="form-control" id="email" name="email" placeholder="Your Email" required pattern="^[a-zA-Z0-9._%+-]{3,}@[a-zA-Z0-9.-]{2,}\.[a-zA-Z]{2,}$" title="Email must have at least 3 characters before @, and at least 2 characters between @ and .">
        </div>
        <div class="form-group mb-3">
          <span class="icon">🔒</span>
          <input type="password" class="form-control" id="password" name="password" placeholder="Password" required pattern="^(?=.*[A-Za-z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$" title="Password must be 8 characters long, including a letter, a number, and a special character.">
        </div>
        <div class="form-group mb-3">
          <span class="icon">🔑</span>
          <input type="tel" class="form-control" id="phone" name="phone" placeholder="Enter your number" required pattern="^\d{10}$" title="Phone number should be 10 digits.">
        </div>
        <div class="form-group mb-3">
          <p class="mt-3">Already have an account? <a href="/auth/login">Login</a></p>
        </div>
        <button type="submit" class="btn btn-primary">Register</button>
      </form>
    </div>
  </div>

  <script>
    // Additional JS for further validation or actions can go here
  </script>
</body>
</html>
